/**
 * Types for SimData resources that pair with tuning types.
 */
enum SimDataGroup {
  Buff = 0x0017E8F6,
  Trait = 0x005FDD0C,
}

// `export default enum` not supported by TS
export default SimDataGroup;
